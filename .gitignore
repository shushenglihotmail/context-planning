node_modules/
*.log
.DS_Store
.idea/
.vscode/

# OS / editor noise
Thumbs.db
*.swp

# cp dogfoods itself: .planning/ IS tracked here so contributors can see
# how cp's own milestones / phases / SUMMARYs evolve. Other harnesses'
# scratch dirs (.claude/, .copilot/) are not part of cp's source tree.
.claude/
.copilot/

# Test scratch
test/__tmp_*

# cp self-install artifacts (cp install copilot writes here for dogfooding;
# these are not part of the cp source tree).
.github/skills/
.github/context-planning.md
.github/agents/

# c8 coverage output
coverage/
.nyc_output/
