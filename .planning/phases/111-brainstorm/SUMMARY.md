Brainstormed v1.10 scope with user (skill probing + verify gate). Locked: parens-sigil syntax, strict-by-default + fuzzy-only-when-no-SP-match policy, role-filtered token-overlap fuzzy algorithm with threshold 0.5, 5-step resolution chain (sigil-fuzzy -> exact -> manual prompt inline -> subagent-dispatch -> bare prompt), consolidated workflow-start diagnostic, new cp run-verify scaffold subcommand with behavior.test_command + auto-detect chain, verify phase inserted before review with depends_on. Verified non-bugs: STATE.md rollover (already v1.9), empty finalize (by-design), auto-stage .planning (policy). Wrote spec to docs/superpowers/specs/2026-06-02-v1.10-skill-routing-verify-gate-design.md and milestone DESIGN.md; refreshed MILESTONE-CONTEXT.md.

invoked_skill: brainstorming
