---
title: "Tests: unit + integration coverage for optimizable"
outcome: Updated test/unit-fanout.js (38 assertions) and test/integration-fanout-v12.js (27 assertions) for the new contract. Removed the old 'partial depends_on falls back silently' test; replaced with explicit 'optimizable:false ignores declared depends_on' and 'optimizable:true with missing depends_on treats as []' tests. Added a legacy 'bare items array' coverage row so back-compat is locked in by test. All fan-out tests green; full npm test suite green.
key-decisions:
  - Kept the legacy bare-array test alongside the wrapped-object tests rather than deprecating it — back-compat is a real contract for any v1.1 caller upgrading.
  - Renamed scenario C from 'partial depends_on -> array fallback' to 'no-optimizable -> array mode' to reflect that the trigger is now the missing top-level flag, not the partiality of per-item edges.
key-files:
  - path: test/unit-fanout.js
    change: modified
    note: 38 assertions; DAG-mode tests pass wrapped object; added legacy + optimizable:false coverage
  - path: test/integration-fanout-v12.js
    change: modified
    note: 27 assertions; rewrote scenarios A/B/C and error paths for wrapped contract
phase: 52
plan: 52.5-03
completed: 2026-05-26
end-commit: e2d4c55afe6df11ddc09cf4fff3de2ecf01f0e7a
---
# Summary 52.5-03

Plan 52.5-03 completed.
