# Inbox

Quick captures awaiting triage. Use `cp capture "..."` to add an item,
`cp inbox` to list, and the `/cp-capture` slash command to process them
interactively (route each to a quick task, phase, seed, or discard).

## Open

- [ ] [2026-05-28T22:19] Restrict workflow template parameterization to a whitelist (skill, prompt, description, max_children, min_children); forbid {{item.X}} and any unresolved {{...}} tokens after expansion. Audit built-in templates (workflows + phases) and migrate any non-conforming usage. Decide whether the same rule applies to phase templates. Origin: cp-quick docs.yaml work surfaced silent literal-string bug from un-templated {{item.id}}.
- [ ] [2026-05-30T11:11] YAGNI: explored adding user-defined parallel route groups to workflows (third parallelism mechanism). Concluded the v1.3 `template:` (workflow-template inclusion) entry already provides exactly this — parallel sibling routes by default, route-on-route DAG via `after:`, nesting with depth limit, multi-instance via `id:` + `args:`. Run `add-user-defined-parallel-phases-in-workflow` abandoned. If discoverability/DX is the real pain, reach for docs/examples first (see _fixtures-v13/), not engine work.
- [ ] [2026-05-31T14:50] cp workflow inspect: wave deduction wrongly elevates phases with 'parent:' field to top-level waves. They should be hidden under the parent and only fanned out at materialize time. Related to Phase 98 supervisor-dispatch work.
- [ ] [2026-06-02T14:24] milestone-seed: v1.10 — downstream retro: skill probing + verification gate. Two real framework bugs surfaced by sibling repo's v4.7 retrospective (verified against current code in this session). (1) lib/provider.js resolveSkill checks provider installation but not skill-level existence in the provider catalog — so configured routings to skills not present in the installed harness silently emit invoke skill: <name> lines that fail at runtime and force (inline-fallback) attestations. Fix: stat <provider-skill-dir>/<name>/SKILL.md inside resolveSkill; on miss, mark installed: false for that skill and surface ONE warning at workflow start (not per-wave). (2) templates/workflows/milestone.yaml review phase is prose-only — no test/smoke gate; downstream shipped a runtime TypeError with full attestation because code-review agent reads code, doesn't run it. Fix: add a verify scaffold phase before/with review that runs configurable test_command (default chain: npm test, pytest, etc.); needs behavior.test_command config key with opt-out for prose-only milestones. ALSO BUNDLE: README PowerShell-on-Windows blurb (cp aliases to Copy-Item; use cp.cmd) — trivial doc fix. NON-FIXES already verified: (3) STATE.md rollover — already fixed in v1.9.0 Bug C (commit 92d14a9, phase 107). (4) empty finalize prompt is by-design (kind: scaffold). (5) auto-stage .planning is per-project policy. Origin: downstream v4.7 retro forwarded 2026-06-02.
- [ ] [2026-06-02T14:47] milestone-seed: v1.10 — downstream retro: skill probing + verification gate (REVISED design after Q&A). === ITEM 1: per-skill availability check + fuzzy opt-in (HIGH value, ~200 lines). Bug: lib/provider.js resolveSkill checks if PROVIDER is installed but not whether the specific SKILL.md file exists in the provider's catalog. Result: configured routings to non-existent skill names silently emit invoke skill: <name> directives that fail at runtime → forced (inline-fallback) attestations per wave. DESIGN: (a) Detection — at workflow start, after template expansion, walk the expanded phase DAG, collect distinct (role, skill_name) pairs (O(unique-skills) ≈ 3-5 stat calls for typical workflows; fan-out/templates do NOT amplify since children inherit parent's skill literal). (b) Per-skill resolution chain: (1) sigil-fuzzy if skill_name contains '*' → glob-match against role-filtered catalog, pick shortest deterministic match, INFO line with candidates; (2) exact match → use it silently; (3) manual provider's prompts[role] → inline prompt baked into wave instruction; (4) generic subagent-dispatch directive (harness-agnostic 'dispatch a subagent with role: <role>'); (5) bare phase prompt fallback. (c) Fuzzy sigil syntax: '*code-review*' substring, 'code-review*' prefix, '*code-review' suffix — standard glob; requires YAML quoting. Role-filter the candidate set using SKILL.md role: frontmatter so '*review*' in a reviewer role doesn't pick a roadmap-reviewer in a planner role. (d) Diagnostic — ONE consolidated block at workflow start (✓ resolved / ℹ fuzzy-matched-to / ⚠ fell-back-to-manual), with copy-pasteable cp config set hint for each warn. No per-wave attestation noise. (e) cp NEVER auto-installs, auto-rewrites config, or auto-fuzzy-matches without sigil opt-in. Whole runtime fallback is autonomous (no user prompts mid-workflow). === ITEM 2: runtime verification gate (HIGH value, ~120 lines + YAML). Bug: templates/workflows/milestone.yaml review phase is prose-only — code-review agent reads code but doesn't run it, so st.write(help=) TypeError shipped with full attestation. DESIGN: Two independent layers. Layer A (code-review quality) is subsumed by item 1's fallback chain — if review_skill is missing, the manual provider's review prompt + subagent-dispatch directive replaces it. Layer B (runtime executable check) needs new pieces: (i) new cp run-verify subcommand in bin/commands/run-verify.js that reads behavior.test_command from .planning/config.json with default fallback chain (npm test → pytest → cargo test → go test ./... → noop with WARN if none detected); exits non-zero on test failure. (ii) new erify scaffold phase in milestone.yaml inserted BEFORE the review phase, kind: scaffold, command: 'cp run-verify {{milestone_slug}}'. (iii) review phase grows depends_on: [verify] so it can't run until tests pass. (iv) opt-out via behavior.verify: false in config or --skip-verify CLI flag for docs-only/prose milestones. (v) per-project test_command override always wins over auto-detection. Crucially scaffold kind = real CLI exec, no LLM in the loop, can't be fake-attested past. === BUNDLED DOCS FIX: README PowerShell-on-Windows blurb (cp aliases to Copy-Item built-in; users must invoke cp.cmd explicitly). === NON-FIXES already verified in this session: STATE.md rollover already fixed v1.9.0 Bug C (commit 92d14a9, phase 107) — downstream user was on pre-v1.9 cp; empty finalize prompt is by-design (kind: scaffold runs cp milestone-finalize); auto-staging .planning is per-project policy not framework bug. === SCOPE: comfortable single-milestone (4-5 phases): (P1) provider.js skill-existence + fuzzy, (P2) runtime.js workflow-start diagnostic + per-skill fallback chain, (P3) cp run-verify command + lib/verify.js + config schema, (P4) milestone.yaml verify phase insertion + tests, (P5) docs + PowerShell blurb + CHANGELOG. Origin: downstream v4.7 retro forwarded 2026-06-02, design refined through interactive Q&A 2026-06-02. SUPERSEDES inbox #4.

## Triaged

- [x] [2026-05-20T12:59] → phase 35: per-phase DESIGN.md template — first-class home for SP brainstorm/discuss output (v0.9 Onboarding milestone)
- [x] [2026-05-21T11:17] → phase 32: /cp-map-codebase auto-init when .planning/ missing (v0.9 Onboarding)
- [x] [2026-05-21T11:17] → phase 33: new cp update command — case 4 onboarding (v0.9 Onboarding)
- [x] [2026-05-21T11:17] → phase 34: README 4-row onboarding decision matrix (v0.9 Onboarding)
- [x] [2026-05-21T11:25] → phase 33 (superseded by #7): cp upgrade per-repo-only design — REJECTED in favor of GSD-style npx one-liner
- [x] [2026-05-21T11:39] → phase 33 (superseded by #7): cp upgrade npx-fronted design v1 — superseded by RENAME LOCK
- [x] [2026-05-21T11:41] → phase 33: RENAME + BEHAVIOR LOCK — command is `cp update` with `npx -y --package=context-planning@latest -- cp update` one-liner
- [x] [2026-05-28T07:28] → BUG/cp: complete-milestone verify failed for v1.5 phase shape (`plansTotal===0` + phase-level SUMMARY.md). Fixed in `lib/milestone.js:verifyMilestoneComplete` by accepting two completion shapes (pre-v1.5 checklist + v1.5 pass-through). See `.planning/quick/2026-05-28-find-the-verify-warning-recorded-for-future-fix-from-last-milestone-v1-5-role-skill-semantics-complete-time-and-investigate-what-s-going-on/SUMMARY.md`.
